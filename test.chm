// CHM test source code

char * (* b(int first, int* second))(){
}

<a>
a just_pass(a* first) {
	int second;
	second = first;
	switch (first) {
		case 10:
		case 20:
		case 30:
		break;
	}
	first = first + second;
	return first;
}


int main(void) {
	just_pass(5, 10, 20);
	return 0;
}
